/// <reference types="react" />
import { Color } from 'react-audio-visualizers-core';
interface RoundBarProps {
    position: [number, number];
    height: number;
    width: number;
    color?: Color;
}
export declare const RoundBar: ({ position: [x, y], height, width, color, }: RoundBarProps) => JSX.Element;
export {};
