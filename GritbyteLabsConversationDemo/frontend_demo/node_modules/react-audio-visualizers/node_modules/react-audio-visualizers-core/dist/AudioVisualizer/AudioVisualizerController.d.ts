import { Audio } from '../types';
import { AudioVisualizerContext } from './AudioVisualizerProvider';
export declare class AudioVisualizerController {
    private audioContext;
    private audioSource;
    private audioBuffer;
    private gain;
    private analyser;
    private context;
    private startedAt;
    private pausedAt;
    constructor(context: AudioVisualizerContext, smoothingTimeConstant?: number, fftSize?: number);
    loadAudio(audio: Audio, setLoading: (loading: boolean) => void): Promise<void>;
    play(): void;
    pause(): void;
    setVolume(volume: number): void;
    clean(): void;
}
