export declare class AudioVisualizerUtils {
    static map(n: number, start1: number, stop1: number, start2: number, stop2: number): number;
    static filterFrequencies(lowFrequency: number | undefined, highFrequency: number | undefined, dataArray: Uint8Array, sampleRate: number): Uint8Array;
    static getFrequencyInterval(lowFrequency: number | undefined, highFrequency: number | undefined, nBars: number, dataArray?: Uint8Array, sampleRate?: number): number;
}
